image: docker:latest

before_script:
  - docker info

services:
  - docker:18.09.1-dind
  

build:
  stage: build
  only:
    - Event
  script:
    -docker build -t hello-world
    - docker tag hello-world vsr-kub005.informatik.tu-chemnitz.de:5000/hello-world
    - docker push vsr-kub005.informatik.tu-chemnitz.de:5000/hello-world
