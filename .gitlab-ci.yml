image: docker:latest

before_script:
  - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"

services:
  - docker:18.09.1-dind

build:
  stage: build
  only:
    - master
  script:
    - docker build -t frontend:latest frontend/.
    

test:
  stage: test
  only:
    - master
  script:
    - docker tag frontend:latest rakibul56/frontend:latest
deploy:
  stage: deploy
  only:
    - master
  script:
    - docker push rakibul56/frontend:latest
   