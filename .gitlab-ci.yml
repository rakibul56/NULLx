image: docker:latest

before_script:
  - docker info

services:
  - docker:18.09.1-dind

build:
  stage: build
         
  only:
    - master
  script:
    - docker build -t event-bustimage:latest event-bus/.
    - docker run -t event-busimage:latest event-bus/.
    
    
   

